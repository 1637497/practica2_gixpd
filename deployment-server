apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-deployment
  labels:
    app: appxd
spec:
  replicas: 3
  selector:
    matchLabels:
      app: appxd
  template:
    metadata:
      labels:
        app: appxd
    spec:
      containers:
      - name: model-server
        image: model-server:default
        env: 
        - name: MODEL_PATH
          valueFrom: 
            configMapKeyRef:
              name: configmap
              key: MODEL_PATH
        resources:
          limits:
            cpu: "500m"
            memory: "128Mi"
          requests:
            cpu: "250m"
            memory: "64Mi"
        volumeMounts:
        - name: model-data
          mountPath: /tmp
      volumes:
      - name: model-data
        hostPath: 
          path: /tmp
          type: Directory
